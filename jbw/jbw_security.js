function checkCredentials(res) {
	//check credentials and either return true, if their session is good, or dump the login form and return false
	if(false) {
		jbw_menu.insert(res, false);
	}else{
		return true;
	}
}

exports.checkCredentials = checkCredentials;